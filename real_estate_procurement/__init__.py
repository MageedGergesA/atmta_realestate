# Wave 11 — this module defines no Python. It is an empty compatibility shell:
# every capability it once owned now lives in its own module, and this manifest
# exists only so installed databases keep a module by this name and keep
# pulling the whole procurement chain through its dependencies.
