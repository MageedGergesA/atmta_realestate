<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_lost_reason_list" model="ir.ui.view">
        <field name="name">realestate.lost.reason.list</field>
        <field name="model">realestate.lost.reason</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="action_lost_reason" model="ir.actions.act_window">
        <field name="name">Lost Reasons</field>
        <field name="res_model">realestate.lost.reason</field>
        <field name="view_mode">list</field>
    </record>

</odoo>
