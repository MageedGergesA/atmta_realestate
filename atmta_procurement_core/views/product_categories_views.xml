<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Wave 11 — moved from `real_estate_procurement`. The categories this
         action lists are seeded by this module's `data/product_categories.xml`. -->
    <record id="action_realestate_product_categories" model="ir.actions.act_window">
        <field name="name">Product Categories</field>
        <field name="res_model">product.category</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
